name: Deploy to GitHub Pages

# The app is static, so "build" only means regenerating the single-file bundle
# and gathering what the site actually serves.
on:
  push:
    branches: [claude/spanish-michel-thomas-app-eg0drl]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a running deploy finish rather than cancelling it half-published.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Build the site
        run: node build.js

      - name: Check the build produced something usable
        run: |
          test -s _site/index.html
          test -s _site/spanish-out-loud.html
          test -s _site/data/curriculum.js
          test -s _site/data/reference.js
          grep -q 'id="app"' _site/index.html
          # Stale assets are how a successful deploy still looks unchanged in a
          # browser that has been here before, so treat an unstamped
          # reference as a build failure rather than a cosmetic detail.
          grep -q 'js/app.js?v=' _site/index.html
          grep -q 'css/styles.css?v=' _site/index.html
          echo "site assembled:"
          find _site -type f | sort

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
